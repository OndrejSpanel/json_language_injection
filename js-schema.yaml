---
properties:
  gestures:
    properties:
      plan:
        type: string
        x-intellij-language-injection:
          language: JavaScript
          prefix: |
            /** @template X */
            class MyPromise {
              /**
               * @template U
               * @param {((value: X) => U) | ((value: X) => MyPromise<U>)} f
               * @returns {MyPromise<U>}
               */
               then(f){}
            }
          suffix: ''
    required:
      - plan
    type: object
type: object
